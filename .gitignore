.vercel
.env*
.gstack/
node_modules
dist

# Page entry files are generated from src/pages.config.js by scripts/gen-pages.mjs.
# Edit the manifest, not these. Regenerate with `npm run gen`.
/*.html

# robots.txt and sitemap.xml are generated by the same script, and their contents
# depend on the build environment (production vs preview). Regenerate, don't commit.
/public/robots.txt
/public/sitemap.xml
/.prerender
