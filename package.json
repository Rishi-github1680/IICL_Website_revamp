{
  "name": "iicl-premium-3d-models",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "gen": "node scripts/gen-pages.mjs",
    "dev": "npm run gen && vite --host 0.0.0.0",
    "build": "npm run gen && vite build",
    "preview": "npm run gen && vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "three": "0.179.1"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "svelte": "^5.56.7",
    "vite": "7.1.5"
  }
}
